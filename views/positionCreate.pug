extends layout

block content
  h1 #{title}

  form(method='POST' action='')
    div
      label(for='position') Position:
      input#position(type='text', name='position' value=(undefined===position ? '' : position.position))
    button(type='submit') Submit

  if errors
   ul
    for error in errors
     li!= error.msg
